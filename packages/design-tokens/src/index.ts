/**
 * @vethis/design-tokens — tokens da marca Vethis (valores TS + CSS vars) e
 * preset Tailwind. Fonte de verdade: VethisDesignSystem.
 */
export { colors, fonts, radius, space } from './tokens';
export { vethisPreset } from './tailwind-preset';
